# Yux 开发计划（超级详细版）

> 目标：把 `01`~`05` 的设计文档落地为 **可用的 v0.1.0**：
> `yuxc` 编译器（Lexer/Parser/Sema/IR/ASM 后端 + 插件 API + build.yml 构建管线）、
> YuxPlugin SDK（语言扩展 + 运行时）、Home Paper 插件示例、混合项目示例。
> 前置阅读：`00`（ADR）、`01`（语法）、`02`（编译器）、`03`（SDK）、`04`（示例）、`05`（混合项目）。

## 目录

1. 目标与范围
2. 技术栈与工具链
3. 版本与发布策略
4. 里程碑总览（M0~M12）
5. 详细任务分解（逐里程碑、逐任务）
6. 关键路径与依赖关系
7. 测试与质量策略
8. 风险与缓解
9. 仓库结构与落地清单
10. 周粒度执行时间表

---

# 1. 目标与范围

## 1.1 版本定义

**v0.1.0（首个可用版本）** 必须交付：

1. `yuxc` CLI：`build / run / test / repl`（repl 可后置）。
2. 覆盖 `01` 语法规范的**核心子集**（§1.2 明确定义包含/排除）。
3. JVM 后端：ASM 直接生成 `.class`（ADR-04）。
4. 编译器插件 SPI：可被 `yux-compiler-minecraft` 加载（02-§10）。
5. `build.yml` → Gradle 构建管线（ADR-12）。
6. `yux.core`/`yux.collection`/`yux.json`/`yux.io`/`yux.async`/`yux.di`/`yux.test` 最小实现。
7. YuxPlugin SDK：`plugin/event/command/config/permission/task` 语法 + 运行时。
8. Home 插件示例（`04`）与混合项目示例（`05`）编译运行通过。

**明确不做（v0.1 范围外）**：

- Native/WASM 后端（只留接口占位，02-§13）。
- 完整 SSA/优化器（只做常量折叠/死代码消除，02-§8.4）。
- `unsafe`/`native fun`/`stack` 的完整语义（只做语法接受 + JVM 降级映射）。
- KMP、Android/iOS 目标。
- 类级增量编译（先文件级）。

## 1.2 语法覆盖矩阵（v0.1 目标）

| 章节（01） | 状态 | 说明 |
|---|---|---|
| §2 词法 | ✅ 全量 | 含插值、三引号、`_` 分隔、十六/二进制 |
| §3 程序结构 | ✅ 全量 | package/import/StmtEnd |
| §4.1 基础类型 | ✅ | Int/Long/Float/Double/Boolean/Char/String/Byte/Any/Unit/Nothing/Range |
| §4.2 可空类型 | ✅ | 空守卫（§8.1） |
| §4.3 泛型 | ✅ | 位置参数 + 擦除 + Signature |
| §4.4 函数类型 | ✅ | `(T)->U`，映射 FunctionN 接口 |
| §4.5 局部推断 | ✅ | 双向收紧，参数显式 |
| §5.1~5.3 类/data/属性 | ✅ | 构造器、访问器、自动生成方法 |
| §5.4 service | ✅ | `@YuxService` + `yux.di` 容器 |
| §5.5~5.7 函数/顶级属性/导入 | ✅ | 参数默认值、文件类、`import .*` |
| §6.1~6.6 语句 | ✅ | if/when/for/while/return/异常 |
| §6.7 async/parallel | ⚠️ 部分 | `async fun`/`async{}` 编译为协程；`parallel{}` 用 ForkJoinPool |
| §7 表达式 | ✅ | 全部优先级、无括号调用、Lambda（含 `it`）、构造调用 |
| §8.1 空安全 | ✅ | 编译提醒 + 运行保护 |
| §8.2 注解 | ✅ | 自定义/内置注解 |
| §8.3 序列化 | ✅ | JSON（Jackson）、YAML（SnakeYAML）、NBT（SDK 层） |
| §8.4 并发 | ⚠️ 部分 | await 同步等待先实现，真正挂起协程后置 |
| §8.5 互操作 | ✅ | JavaBean、静态成员、import |
| §8.6 内存 | ⚠️ 语法接受 | stack/unsafe 语法接受 + JVM 降级 |
| §8.7 继承/接口 | ✅ | extends/implements/override |
| §9 扩展点 | ✅ | 编译器插件 SPI + minecraft 扩展 |
| §10 标准库 | ✅ 最小集 | 见 §9 本计划的库优先级 |

---

# 2. 技术栈与工具链

| 类别 | 选择 | 版本 | 说明 |
|---|---|---|---|
| JDK | Temurin | 21 | target.jvm=21（ADR-12 配套） |
| 构建 | Gradle | 8.10+ | 编译器自身工程 + 生成脚本 |
| 编译器语言 | Kotlin | 2.0.x | ADR-02 |
| 字节码 | ASM | 9.7 | `asm` + `asm-commons` |
| JSON | Jackson `databind` | 2.17.x | yux.json / 序列化 |
| YAML | SnakeYAML | 2.2 | yux 配置 / SDK config |
| 协程 | kotlinx-coroutines | 1.8.x | async 后端运行时 |
| DI | 自研（轻量） | — | yux.di，避免引入重量框架 |
| 测试 | JUnit 5 + kotlin.test | — | 单测 |
| 快照测试 | 自研 golden 工具 | — | 词法/语法/IR 快照 |
| Paper API | paper-api | 1.21-R0.1-SNAPSHOT | SDK 编译期依赖 |
| 命令 YAML 校验 | SnakeYAML | 复用 | build.yml 解析 |

**约定**：
- 编译器自身用 Gradle 8 构建；**产物 `yuxc` 依赖的运行时 jar** 与目标项目区分，目标项目使用 `target.jvm` 指定的 JDK。
- 依赖尽量少：避免引入重型框架（不用 KSP/KAPT）。
- 所有第三方依赖版本集中写在 `gradle/libs.versions.toml`（Version Catalog）。

---

# 3. 版本与发布策略

- 采用语义化版本 `MAJOR.MINOR.PATCH`：v0.x 阶段 `PATCH` 允许破坏（预发布）。
- 版本节奏：里程碑结束即打 tag（`v0.1.0-m0`、`v0.1.0-m1` …），`v0.1.0` 为 GA 候选。
- 制品：
  1. `yuxc` 发行包（zip/tgz：bin + lib）。
  2. 编译产物发布到 `mavenLocal()`（开发期），后续 Nexus/Artifactory。
  3. `yux-stdlib`、`yux-plugin-minecraft`、`yux-minecraft-runtime` 独立坐标。
- `CHANGELOG.md` 随里程碑更新，记录 breaking changes（预发布阶段需明确说明）。

---

# 4. 里程碑总览（M0~M12）

| 里程碑 | 主题 | 主要交付物 | 依赖 | 预估 |
|---|---|---|---|---|
| **M0** | 工程骨架 + CI | 多模块 Gradle 工程、lint、CI 跑通 | — | 0.5 周 |
| **M1** | Lexer | Token/Trivia/插值分词 + golden 测试 | M0 | 1 周 |
| **M2** | Parser | CST/AST、Pratt 表达式、消歧、错误恢复 | M1 | 2 周 |
| **M3** | Sema | 符号表、类型检查、局部推断、智能转型、空守卫 | M2 | 2.5 周 |
| **M4** | IR | IrModule/Class/Method/Stmt/Expr + 最小优化 | M3 | 2 周 |
| **M5** | JVM 后端 | ASM 发射、类/属性/data/Lambda/注解、`yuxc run` | M4 | 2.5 周 |
| **M6** | 插件 API | `yux-compiler-plugin-api` SPI + 加载机制 | M5 | 1 周 |
| **M7** | 构建管线 | build.yml 解析、Gradle 生成、编译顺序编排 | M5 | 2 周 |
| **M8** | YuxPlugin SDK | 语言扩展 + 运行时 + NBT + 调度 | M6+M7 | 2.5 周 |
| **M9** | Paper 示例 | Home 插件（04）编译运行 + 集成测试 | M8 | 1 周 |
| **M10** | 混合项目 | 三方互操作示例（05）跑通 | M7+M8 | 1 周 |
| **M11** | 标准库完善 + 泛化测试 | yux.io/net/async 补齐、fuzz、基准 | M5 | 1.5 周 |
| **M12** | 发布 v0.1.0 | 文档收口、打包、`v0.1.0` tag、示例视频/README | 全部 | 1 周 |

**关键路径**：M0→M1→M2→M3→M4→M5→M6→M7→M8→M9（→M10 可并行 M9）。总计约 17.5 周（单人全职）；多人可并行 M6/M7 与 M3 之后的测试。

---

# 5. 详细任务分解

> 每个任务格式：`T-<里程碑>-<序号>` + 输入/动作/输出/验收。**验收标准必须可执行（命令 + 期望输出）**。

## M0 工程骨架与 CI

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M0-1 仓库初始化 | 建 Git 仓库、`.gitignore`、`LICENSE`、README 骨架 | `README.md`、`.gitignore` | `git init` 后结构完整 |
| T-M0-2 Gradle 多模块 | 按 02-§14 建 6 个模块；Version Catalog 集中依赖 | `settings.gradle.kts`、`gradle/libs.versions.toml` | `./gradlew projects` 列出全部模块 |
| T-M0-3 编码规范 | Kotlin lint（ktlint/detekt 二选一）、EditorConfig | `detekt.yml`、`.editorconfig` | `./gradlew lint` 通过 |
| T-M0-4 CI | GitHub Actions：PR 触发 `build + test + lint + jacoco 覆盖率门禁(≥60%)` | `.github/workflows/ci.yml` | 空 PR 绿 |
| T-M0-5 测试基座 | 建 `yux-test-support` 模块：golden 工具 + 内存源文件工具 | `YuxTestFixtures` | 样例测试可跑 |
| T-M0-6 tag | — | `v0.1.0-m0` | 打 tag |

## M1 词法分析器

设计依据：01-§2、02-§4。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M1-1 Token 定义 | `TokenKind`、`Token(position, kind, text, trivia)`、`SourcePosition` | `lexer/Token.kt` | 单测：类型枚举与 01-§2.6 运算符表一致 |
| T-M1-2 标识符/关键字 | 标识符扫描、30 基础关键字 + 软关键字表、大小写 | `lexer/Lexer.kt`（部分） | 关键字表 == 01-§2.4（30 个） |
| T-M1-3 数字字面量 | 十/十六/二进制、`_` 分隔、`L/F/D` 后缀、浮点 | 同上 | golden：`1_000L`→Int?→Long；`0x1F`；`3.14F` |
| T-M1-4 字符/字符串 | 转义表（01-§2.5）、`\$`、`"""` 原始串 | 同上 | golden：`"\n\t\u0041"`、`"""$x"""` 不插值 |
| T-M1-5 插值分词 | `$name`、`${expr}` 括号平衡、嵌套字符串 | `lexer/InterpolationScanner.kt` | 单测：`"$a${b+c}"` 产 `INTERPOLATION_START`+`IDENT`+… |
| T-M1-6 注释/Trivia/换行 | `//`、`/* */`（可嵌套）、`NEWLINE` 折叠、DocComment 保留 | 同上 | 单测：`/* /* nested */ */`；连续换行折叠为一个 |
| T-M1-7 错误恢复 | 非法字符/未闭合字符串 → ERROR 诊断 | `diag/Diagnostic.kt` | 单测：坏输入不抛异常、返回诊断 |
| T-M1-8 快照测试 | golden：为 01 全部示例源码建 `.tokens` 快照 | `yux-test-support/GoldenTest.kt` + 用例 | `./gradlew :core:test` 全绿 |

**M1 验收命令**：
```
./gradlew :yux-compiler:yux-compiler-core:test
# 期望：golden 快照全部匹配，0 失败
```

## M2 语法分析器

设计依据：01-§3、§6、§7、§7.7；02-§5、§6。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M2-1 CST 定义 | 全产生式 CST 节点（含括号/换行/注释占位） | `parser/Cst.kt` | 与 01-§3 文法一一对应（逐产式清单核对） |
| T-M2-2 Pratt 表达式 | 前缀/中缀绑定力表（01-§7.1 优先级表）、后缀 `.`/call/`?` | `parser/PrattParser.kt` | 单测：优先级 `f a + b == (f(a)+b)`；`a.b.c` 链 |
| T-M2-3 语句解析 | 全部语句（if/when/for/while/try/async/parallel/unsafe）；StmtEnd 换行/`;` | `parser/StatementParser.kt` | 单测：块内最后语句省略换行；单行 `if x y` |
| T-M2-4 声明解析 | 类/data/service/函数/属性/访问器/typeparams/extends/implements | `parser/DeclarationParser.kt` | 覆盖 01-§5 全部示例 |
| T-M2-5 消歧（预收集） | 第一遍：声明收集；第二遍：`resolveIdentifierChain`（01-§7.7） | `parser/Disambiguation.kt` | 单测：`Result Int(x)` 构造 vs `send "Hi"` 调用 vs `Result(x)` 构造 |
| T-M2-6 Lambda | 箭头 lambda、`(a,b)->`、`it` 绑定、块参数 lambda（`{ it.name }`） | `parser/LambdaParser.kt` | 单测：`map { it.name }`；`(a,b->a+b)` |
| T-M2-7 无括号调用 | `Argument` 为单一 Primary 的绑定规则（01-§7.2.2） | 并入 T-M2-2 | 单测：`player.send "x"`；`f x + 1 == f(x)+1` |
| T-M2-8 错误恢复 | 同步恢复（跳过到 NEWLINE/}）、多错误收集、格式化诊断 | `parser/Recovery.kt` | 单测：缺括号/缺 `)` 时仍报出后续错误 |
| T-M2-9 CstToAst | 规约 AST 节点 `YxNode(span)` | `parser/CstToAst.kt` | 单测：AST dump 与期望一致 |
| T-M2-10 快照测试 | 01 全部示例 → `.ast` 快照 | golden 用例 | `./gradlew test` 全绿 |

**M2 验收命令**：
```
./gradlew :yux-compiler:yux-compiler-core:test
# 期望：含消歧专项用例；01 语法示例 100% 可解析
```

## M3 语义分析

设计依据：01-§4.5、§5、§6、§8；02-§7。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M3-1 类型系统 | `YxType` 层次（02-§8.1 的 IrType 先行等价物）、映射 JVM | `sema/types/` | 单测：Nullable/Generic/Function 组合 |
| T-M3-2 符号表 | GlobalScope→File→Decl→Block 层级；JVM 类懒加载 `ClassPathSymbolProvider` + 缓存 | `sema/SymbolTable.kt` | 单测：跨文件 import、`import .*`、冲突检测 |
| T-M3-3 声明解析重写 | 两遍式：收集→校验（02-§6.2） | `sema/Declarations.kt` | 单测：重复声明报错 |
| T-M3-4 类型检查 | 全部 S-xx 规则：条件 Boolean、参数匹配、确定性赋值、泛型元数、访问器合法 | `sema/TypeChecker.kt` | 单测：覆盖 01 每章错误示例 → 期望诊断码 |
| T-M3-5 局部推断 | 约束收集+双向传播+unify；失败定位（01-§4.5） | `sema/Inference.kt` | 单测：`name="S"`、`scores:List Int=[]`、推断失败样例 |
| T-M3-6 智能转型 | `is` 分支、空守卫后分支、不可变引用才转型 | `sema/SmartCast.kt` | 单测：可变引用转型 → REMIND |
| T-M3-7 空守卫 | 可空接收者 `.b` 降级为守卫 + REMIND 诊断 | `lowering/NullGuard.kt` | 单测：读短路/写不守卫 |
| T-M3-8 data/service 校验 | data 自动成员、service 注入图循环检测 | `sema/Annotations.kt` | 单测：data 生成列表、循环依赖报错 |
| T-M3-9 注解 | 注解解析/应用（01-§8.2）、@Serializable 语义 | 同上 | 单测：注解目标校验 |
| T-M3-10 诊断码表 | 定义 `E0001~` 错误码手册 | `diag/ErrorCodes.md` | 与诊断输出一致 |

**M3 验收命令**：
```
./gradlew test   # 含"错误示例 → 期望诊断"的负向用例 ≥ 80 个
```

## M4 IR

设计依据：02-§8。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M4-1 IrModule 层次 | IrModule/Class/Method/Field/Property（含 backing+getter/setter） | `ir/` | 单测：属性→2 方法映射 |
| T-M4-2 IrStmt/IrExpr | 按 02-§8.2 全集实现（含 NullGuard、StringTemplate、Lambda） | `ir/IrStmt.kt` `ir/IrExpr.kt` | 单测：结构完整 |
| T-M4-3 IrType | 独立 JVM 无关类型（02-§8.1），与 M3 类型桥接 | `ir/IrType.kt` | 单测：类型等价比较 |
| T-M4-4 AST→IR | `Lowering`（语法糖下沉）+ `IRGen` | `lowering/` `irgen/IRGen.kt` | 单测：if→Branch；for→迭代器展开 |
| T-M4-5 最小优化 | 常量折叠、死代码消除、空守卫折叠 | `optimizer/BasicOpt.kt` | 单测：`2+3`→`5`；不可达代码移除 |
| T-M4-6 IR dump | `yuxc ast/ir` 子命令输出可读 IR | CLI 子命令 | 快照测试 `.ir` 文件 |

**M4 验收命令**：
```
yuxc ir src/main.yux
# 期望：输出 IrModule 文本；golden 快照通过
```

## M5 JVM 后端（ASM）✅（v0.1.0-m5）

> 已完成（2026-08-08）：`yux-compiler-backend-jvm` 模块（AsmBackend/AsmEmitter/AsmDataGen/JvmDescResolver/InvocationAdapter）、
> `yuxc run`（Runner + MemoryClassLoader）、`yux-stdlib` 最小运行时（CoreLib/Range/FunctionN/YuxService）、
> `samples/` 七个端到端样例 + `.stdout` 快照。验收命令见下（已通过）。
> 已知限制：LineNumberTable 后置（IR 无源码 span）、Signature 属性后置、`async` 同步降级（R3）。

设计依据：01-§5、§7、§8；02-§9。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M5-0 严格 Git Workflow | 落地单一 feature 分支策略：只保留 `feature/m5-jvm-backend`，删除旧里程碑分支，规则写入 `AGENTS.md` | `AGENTS.md`、§7.4 | `git branch -a` 本地仅当前 feature 分支，远端仅 `origin/feature/<里程碑>` + `origin/main`；PR CI 全绿 |
| T-M5-1 类生成 | ClassWriter(COMPUTE_FRAMES)、类/文件类命名（05-§5.3） | `backend/jvm/AsmBackend.kt` | 单测：`javap -p` 核对类名/访问标志 |
| T-M5-2 字段/属性 | 私有字段 + `getName/setName`（Boolean→`isX`）、自定义访问器 | `AsmEmitter.kt` | 单测：反射验证 getter/setter |
| T-M5-3 方法体发射 | 遍历 IrStmt/Expr 发指令；源码行号表 | 同上 | 单测：`run` 输出正确 |
| T-M5-4 构造/调用/Lambda | 构造调用、静态/实例调用、`invokedynamic` LambdaMetafactory | 同上 | 单测：闭包捕获、`it` lambda 运行正确 |
| T-M5-5 data 类 | equals/hashCode/toString/全参构造器（01-§5.3） | `AsmDataGen.kt` | 单测：`User(1,"S").equals` 等 |
| T-M5-6 注解 | JVM 注解字节码发射 | 同上 | 单测：反射读注解 |
| T-M5-7 互操作 | JavaBean 调用映射（`getName()`→属性）；静态成员 | 并入发射器 | 集成：调用 `System.currentTimeMillis` |
| T-M5-8 空守卫/字符串模板 | NullGuard → ifnonnull/ifnull + 默认值；StringTemplate → StringBuilder | 并入发射器 | 单测：可空链短路；插值正确 |
| T-M5-9 async 后端 | `async fun` → 协程状态机（kotlinx.coroutines）或先同步降级 | `backend/jvm/AsyncGen.kt` | 单测：`async{}` 内 IO 不阻塞主线程（计时） |
| T-M5-10 运行时加载 | `yuxc run`：编译→自定义类加载器→`main` 执行 | `cli/Runner.kt` | 验收命令见下 |
| T-M5-11 端到端 | `run-samples/`：01 示例程序全部可运行 | 示例目录 | 见验收命令 |

**M5 验收命令**：
```
yuxc run samples/hello.yux
# 期望输出: Hello Yux
yuxc run samples/data.yux
# 期望输出: User(id=1, name=Steve)
# 并对 samples/*.yux 逐一端到端比对 stdout
```

## M6 编译器插件 API

设计依据：02-§10。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M6-1 SPI 定义 | `YuxCompilerPlugin`、`PluginContext`、`ExtensionParser` 等 | `plugin-api/` | 单测：接口可编译 |
| T-M6-2 注册通道 | registerKeyword/Transform/SemanticRule/CodegenHook/addRuntimeDependency | `PluginContext.kt` | 单测：调用链记录 |
| T-M6-3 插件加载 | ServiceLoader + `--plugin <jar>` + 冲突/隔离校验 | `core/plugin/PluginLoader.kt` | 单测：加载测试插件 jar |
| T-M6-4 扩展关键字接入 | Parser 在 `ExtensionParser` 钩子处暂停并委托 | `parser/ExtensionParser.kt` | 单测：注入 `event` 关键字可解析 |
| T-M6-5 下沉管线 | 插件节点在 Lowering 前全部下沉（02-§10.4） | `lowering/PluginLowering.kt` | 单测：`event` 节点无泄漏到后端 |
| T-M6-6 示例插件 | 写一个 `hello-extension` 测试插件贯穿全流程 | `samples/extension/` | `yuxc build --plugin ...` 产出预期 |

**M6 验收命令**：
```
./gradlew test   # 含"插件扩展关键字 → 解析 → 下沉 → 字节码"链路用例
```

## M7 构建管线（build.yml → Gradle）

设计依据：02-§11、05-§4。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M7-1 build.yml 解析 | SnakeYAML → `BuildConfig` 模型；schema 校验（缺字段报错） | `build-tool/BuildYmlParser.kt` | 单测：01-§11.2 全字段 |
| T-M7-2 Gradle 生成器 | 生成 build.gradle.kts/settings.gradle（02-§11.3） | `build-tool/GradleGenerator.kt` | 单测：生成文本快照 |
| T-M7-3 编译顺序编排 | javac→kotlinc→yuxc 顺序 + `yux-symbols.json` 映射（05-§4,§8） | `build-tool/BuildPlanner.kt` | 单测：混合项目顺序正确 |
| T-M7-4 Gradle 调用 | 托管运行 Gradle（守护进程/`--offline` 开关）、产物收集 | `build-tool/GradleRunner.kt` | 集成：小项目构建成功 |
| T-M7-5 `yuxc build/run/test` | CLI 子命令接线（02-§14） | `cli/Commands.kt` | 验收命令见下 |
| T-M7-6 文件级增量 | 缓存 `build/yux-cache`、依赖图、仅重编译变更（02-§12.3） | `build-tool/Incremental.kt` | 单测：改一个文件只重编相关 |
| T-M7-7 IDE 映射 | 输出 `yux-symbols.json` 与源码映射（为未来 IDE 插件预留） | 并入 T-M7-3 | 文件存在且格式稳定 |

**M7 验收命令**：
```
yuxc build -p samples/helloworld/   # 期望：build/ 下 jar，plugin 目录产物齐全
yuxc build --clean                   # 全量重建通过
```

## M8 YuxPlugin SDK

设计依据：03 全篇。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M8-1 注解契约 | `@YuxEvent/@YuxCommand/@YuxConfig/@YuxTask` + `PluginBootstrap` | `runtime/annotations/` | 单测：注解目标/保留策略 |
| T-M8-2 语言扩展注册 | minecraft 插件注册 `plugin/event/command/config/permission/task`（03-§3） | `compiler-plugin/` | 单测：`event PlayerJoin(player){}` 解析+下沉（03-§5） |
| T-M8-3 event 下沉 | → `@YuxEvent` + Listener 类；参数绑定（事件对象属性） | 下沉规则 | 单测：`PlayerJoin(player)` → `e.getPlayer()` |
| T-M8-4 command 下沉 | → `@YuxCommand` + executor/tab（03-§5） | 下沉规则 | 单测：permission/aliases 生成 |
| T-M8-5 config 下沉 | → `@YuxConfig` + data 类 + config.yml 默认值（03-§3.5） | 下沉规则 | 单测：默认值→YAML 文本 |
| T-M8-6 permission/task 下沉 | plugin.yml permissions；BukkitScheduler 调用 | 下沉规则 | 单测：repeat/delay/async task |
| T-M8-7 运行时注册 | PluginBootstrap.onEnable：扫描注解→注册事件/命令/配置（03-§4.2） | `runtime/bootstrap/` | 集成（见验收） |
| T-M8-8 ConfigManager | YAML 加载/保存/reload、类型覆盖（03-§4.3） | `runtime/config/` | 单测：字段覆盖、嵌套 data |
| T-M8-9 NBT 序列化 | 03-§6.2 类型映射表；serializeNbt/deserializeNbt | `runtime/serialize/` | 单测：round-trip 全类型 |
| T-M8-10 调度封装 | scheduleSync/task/repeat（03-§9.2）、线程规则 | `runtime/schedule/` | 单测：延迟顺序 |
| T-M8-11 工具 | ItemBuilder/PlayerUtil/LocationUtil（03-§7.3） | `runtime/util/` | 单测：ItemBuilder 链 |
| T-M8-12 plugin.yml 生成 | main/permissions/api-version（03-§11 示例） | 后端钩子 | 单测：文本快照 |

**M8 验收命令**（需要本地 Paper 模拟层或头缓存测试）：
```
# 单元层：全部 runtime 单测绿
# 集成层：用 Paper 的"头less 测试"或本地服务器容器跑 Home 插件加载事件
./gradlew :yux-plugin-minecraft:test
```

## M9 Paper 示例插件（Home）

设计依据：04 全篇。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M9-1 源码落地 | 按 04-§2~§9 完整落地 `samples/home-plugin/` | 全部 .yux | `yuxc build` 成功 |
| T-M9-2 功能验证 | 手动+脚本验证：sethome/home/delhome/homes/tab/欢迎/冷却/定期保存 | 验证脚本 | 见验收命令 |
| T-M9-3 集成测试 | 用 MockBukkit 或 Paper 容器跑事件/命令断言 | 测试目录 | 测试绿 |
| T-M9-4 Java 对照 | 实现 04-§11 的 Java 版等价插件，对比行数 | `samples/java-equivalent/` | 记录行数对比到 README |

**M9 验收命令**：
```
yuxc build -p samples/home-plugin
yuxc test  -p samples/home-plugin
# 期望：4 条命令 + tab 补全 + 2 类事件 + 配置读写全部通过；生成 plugin.yml 与 04-§10 一致
```

## M10 混合项目示例

设计依据：05 全篇。

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M10-1 三方互操作 | 按 05-§5 双向调用示例落地 `samples/mixed/` | 源码 | 编译通过 |
| T-M10-2 编译顺序验证 | javac→kotlinc→yuxc 顺序 + `yux-symbols.json` | 构建日志 | 05-§8 常见问题全部规避 |
| T-M10-3 余额系统 | 05-§9 完整示例跑通 | 运行日志 | 命令输出正确 |

**M10 验收命令**：
```
yuxc build -p samples/mixed && yuxc run -p samples/mixed
# 期望：Kotlin 格式化 + Java 日志 + Yux 命令三方输出正确
```

## M11 标准库完善与泛化测试

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M11-1 yux.io/net | readText/writeText/httpGet/httpPost/fetch | `yux-stdlib/` | 单测：临时文件、mock HTTP |
| T-M11-2 yux.async 完善 | await 真正挂起、launch/Task、parallel 测试 | 同上 | 单测：并发时序 |
| T-M11-3 集合/字符串 | map/filter/sort/reduce/format 等（01-§10.1~10.2） | 同上 | 单测全绿 |
| T-M11-4 fuzz | 词法/语法随机输入（覆盖 5000 例）确保不崩溃 | 工具 | 0 panic |
| T-M11-5 基准 | 编译耗时/输出 jar 体积基准记录到 `bench/` | 基准脚本 | 对比基线入 CHANGELOG |
| T-M11-6 覆盖率 | 核心模块行覆盖 ≥ 70% | CI 报告 | 门禁通过 |

## M12 发布 v0.1.0

| 任务 | 动作 | 输出 | 验收 |
|---|---|---|---|
| T-M12-1 文档收口 | README 快速开始、`docs/` 交叉链接校验、补 `docs/07-已知限制.md` | 文档 | 从零机器按 README 可跑通 Home 插件 |
| T-M12-2 打包 | yuxc 发行包（zip/tgz）、发布到 mavenLocal | 发布脚本 | 干净环境安装成功 |
| T-M12-3 演示 | Home 插件演示 gif/视频 + 一行命令 demo | 演示素材 | 录屏 |
| T-M12-4 tag | `v0.1.0` + CHANGELOG 完整 | tag | 里程碑清单全勾选 |

---

# 6. 关键路径与依赖关系

```
M0 ─► M1 ─► M2 ─► M3 ─► M4 ─► M5 ─► M6 ─► M7 ─► M8 ─► M9 ─► M12
                                   │      │      └──────► M10 ──┘
                                   │      └─► M11 ─────────────► M12
                                   └─► M5 后可直接做 M11(独立)
```

- **严格串行**：M1→M2→M3→M4→M5（编译器核心，不可跳过）。
- **可并行**：
  - M6（插件 API）与 M7（构建管线）彼此独立，可在 M5 完成后并行。
  - M9 与 M10 并行。
  - M11 与 M6/M7 并行（标准库不依赖插件）。
- **里程碑间集成点**：M5 结束做一次「编译器端到端」，M8 结束做一次「SDK 冒烟」，M9/M10 做完整集成。

**依赖外部交付**：
- Paper API 坐标可用性（编译期依赖）；MockBukkit 或本地服务器用于集成测试。
- kotlinx-coroutines、Jackson、SnakeYAML 的仓库可达性（内网需镜像）。

---

# 7. 测试与质量策略

## 7.1 测试金字塔

```
        ▲ 集成（Paper 容器 / 混合项目跑通） 少量
       ▲  端到端（samples 全跑）            中量
      ▲  单测 + golden 快照（词法/语法/IR） 大量
     ▲   编译期负向用例（错误示例→诊断码）  大量
```

- **golden 快照**：`.tokens/.ast/.ir/.class-javap` 四级快照，变更需审阅（CI 有 `UPDATE_GOLDEN=1` 逃生阀）。
- **负向用例**：为 01 每章的错误规则建用例，断言诊断码（T-M3-10 的 ErrorCodes.md 为唯一事实源）。
- **端到端**：`samples/*.yux` 的 stdout 快照，`yuxc run` 比对。
- **集成**：MockBukkit 覆盖 Home 插件事件/命令；混合项目用真实 javac/kotlinc。

## 7.2 质量门禁（CI）

1. `./gradlew build`（含全部测试）
2. detekt 无 error
3. Jacoco：core 行覆盖 ≥ 70%（M11 起）
4. golden 快照 diff 为零
5. `yuxc build` 自举（用 yuxc 构建一个示例工程）

## 7.3 代码评审约定

- PR 拆分按里程碑粒度；每个 PR 含测试 + 快照 + CHANGELOG 行。
- 编译器核心 PR 需「语法↔语义↔后端」三端对应用例。
- 诊断文本改动走「快照更新 + 审阅」流程。
- 严格 Git Workflow（M5 起强制）见 §7.4。

## 7.4 严格 Git Workflow（M5 起）

> 目标：所有开发统一在单一 feature 分支上进行；`main` 只接受 PR 合入；旧里程碑分支随用随删，避免多分支漂移。

### 分支模型

- 当前唯一开发分支：`feature/m5-jvm-backend`；每个里程碑只允许一个 `feature/<milestone>` 分支。
- `main` 是受保护集成分支：禁止直接开发/提交/push，只通过 GitHub PR 合入。
- 不再创建或保留 `m<里程碑>-<主题>` 临时分支；已合入 feature 的内容直接删除本地与远端旧分支。

### 提交规范

- 提交信息以 `T-<里程碑>-<任务>` 开头（如 `T-M5-1: ...`），或使用 `fix:`/`docs:` 等 conventional commit 风格。
- 每个提交/PR 必须可构建且测试通过；测试、golden 快照、CHANGELOG 随变更一起落地。
- 禁止对共享 feature 分支 force push；需要改写历史时先确认。

### PR 与评审

- PR 按里程碑粒度拆分，目标一律为 `main`。
- 必须通过 CI（`./gradlew build lint` + 覆盖率门禁）与 CodeRabbit/人工评审。
- 诊断文本或 golden 快照变更必须同步更新快照，并在 PR 中说明。
- 合并后的旧分支立即删除（本地 + 远端）。

### 验收命令

```
git branch -a
# 期望：本地仅当前 feature 分支；远端仅 origin/feature/<里程碑>、origin/main 与 origin/HEAD
```

---

# 8. 风险与缓解

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| R1 | 无括号调用/消歧（01-§7.7）边缘用例爆炸 | 语法歧义、bug 多 | 先冻结为「两遍符号表消歧」；golden 快照守住回归；解析器 T-M2-5 专项测试 ≥ 40 例 |
| R2 | 空守卫自动插入改变语义（01-§8.1） | 运行时行为不可预期 | 收紧规则：仅读路径 + REMIND；写路径显式；负向用例覆盖 |
| R3 | `async` 编译为协程复杂度高 | M5 延期 | 降级方案：v0.1 先「同步降级 + REMIND」，真正协程列入 M11 |
| R4 | Paper API 编译期依赖/本地服务器不可得 | 集成测试卡壳 | 用 MockBukkit 头less；运行时对 Paper 版本做兼容层 |
| R5 | Kotlin/Java 互操作命名映射（05-§8） | 混合项目失败 | `yux-symbols.json` 导出 + 自动化映射测试；命名规则写入 05 |
| R6 | 范围蔓延（想加语法） | 延期 | 冻结 v0.1 语法覆盖矩阵（§1.2），新增语法列「后置清单」 |
| R7 | 单人全职周期过长 | 失去动力 | 里程碑打 tag、每 2 周可运行 demo；可并行任务分给协作者 |

## 8.1 范围冻结清单（v0.1 后置）

- 运算符重载、扩展函数、`when` 表达式值、密封类型。
- 完整 SSA/优化器、类级增量编译。
- Native/WASM 后端。
- IDE 插件、LSP（`yux-symbols.json` 已预留）。

---

# 9. 仓库结构与落地清单

## 9.1 仓库目录（对应 02-§14 + 04/05 示例）

```
yux/
├── settings.gradle.kts
├── build.gradle.kts
├── gradle/libs.versions.toml
├── docs/                          # 00~06
├── yux-compiler/
│   ├── yux-compiler-core/            # lexer/parser/ast/sema/lowering/ir/diag
│   ├── yux-compiler-backend-jvm/     # AsmBackend/Emitter/DataGen/AsyncGen
│   ├── yux-compiler-plugin-api/      # SPI
│   ├── yux-compiler-cli/             # yuxc
│   └── yux-test-support/             # golden + fixtures
├── yux-plugin-minecraft/
│   ├── yux-compiler-minecraft/       # 语言扩展（编译期）
│   └── yux-minecraft-runtime/        # 运行时
├── yux-stdlib/                       # yux.core/collection/io/net/async/json/di/test
├── build-tool/                       # BuildYmlParser/GradleGenerator/BuildPlanner
└── samples/
    ├── hello/  data/  extensions/
    ├── home-plugin/                  # 04
    └── mixed/                        # 05（java/ kotlin/ src）
```

## 9.2 每模块启动的「入口文件」清单

| 模块 | 首个文件 | 说明 |
|---|---|---|
| core | `lexer/Token.kt` | M1 起点 |
| backend-jvm | `backend/jvm/AsmBackend.kt` | M5 起点 |
| plugin-api | `plugin/api/YuxCompilerPlugin.kt` | M6 起点 |
| build-tool | `BuildYmlParser.kt` | M7 起点 |
| compiler-minecraft | `CompilerPlugin.kt` | M8 起点 |
| runtime | `bootstrap/PluginBootstrap.kt` | M8 起点 |
| stdlib | `core/CoreLib.kt` | M11 起点 |

---

# 10. 周粒度执行时间表

> 单人全职估算。多人时按 §6 并行关系压缩。

| 周 | 里程碑 | 关键产出 | 周目标演示 |
|---|---|---|---|
| 1 | M0 | 骨架+CI | `./gradlew test` 绿 |
| 2-3 | M1 | Lexer+golden | `yuxc lex sample.yux` 出 token |
| 4-5 | M2 | Parser+消歧 | `yuxc ast sample.yux` |
| 6-8 | M3 | Sema | 负向用例全绿 |
| 9-10 | M4 | IR | `yuxc ir` dump |
| 11-13 | M5 | 后端+run | `yuxc run hello.yux` 输出 Hello Yux |
| 14 | M5/M6 | 端到端 | samples 全跑 |
| 15-16 | M6+M7 | 插件API+构建 | `yuxc build -p samples/helloworld` |
| 17-19 | M8 | SDK | `event/command` 语法可用 |
| 20 | M9 | Home 插件 | Home 插件跑通（MockBukkit） |
| 21 | M10 | 混合项目 | 三方互调演示 |
| 22-23 | M11 | 标准库+fuzz | 覆盖率≥70% |
| 24 | M12 | 发布 | `v0.1.0` tag |

## 每周末检查清单

1. 对应里程碑任务全部完成且验收命令通过。
2. golden 快照入库。
3. CHANGELOG 更新。
4. 打 tag `v0.1.0-m<里程碑>`。
5. 更新 §1.2 覆盖矩阵（✅/⚠️/❌）。

---

# 附录 A：验收命令速查

```
./gradlew build lint          # 全量构建 + 规范检查
./gradlew :yux-compiler:yux-compiler-core:test
yuxc lex  src/main.yux         # M1 验收
yuxc ast  src/main.yux         # M2 验收
yuxc ir   src/main.yux         # M4 验收
yuxc run  samples/hello.yux    # M5 验收
yuxc build -p samples/home-plugin   # M8/M9 验收
yuxc test  -p samples/home-plugin
yuxc build -p samples/mixed && yuxc run -p samples/mixed   # M10 验收
```

# 附录 B：与设计文档的索引

| 计划章节 | 设计文档 |
|---|---|
| §1.2 覆盖矩阵 | 01 全文 |
| §2 工具链 | 02-§2, §11 |
| M1 | 01-§2, 02-§4 |
| M2 | 01-§3,6,7,7.7, 02-§5,6 |
| M3 | 01-§4.5,5,6,8, 02-§7 |
| M4 | 02-§8 |
| M5 | 01-§5,7,8, 02-§9 |
| M6 | 02-§10 |
| M7 | 02-§11,12, 05-§4 |
| M8 | 03 全文 |
| M9 | 04 全文 |
| M10 | 05 全文 |
| M11 | 01-§10 |
