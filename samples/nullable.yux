// M5-11 端到端样例：可空类型 + 智能转型（S-8.1）
Sword {
    damage:Int
}

fun main() {
    item:Any? = null
    if item is Sword {
        print "sword"
    }

    name:String? = "Steve"
    if name != null {
        print name.length
    }

    len = name.length
    print len
}
