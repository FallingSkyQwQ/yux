// 03-精通 · 第 5 章：自动序列化（Json serialize/deserialize 往返）
import yux.json.Json
data User {
    id:Int
    name:String
}
fun main() {
    u = User(1, "Steve")
    s = Json.serialize(u) as String
    println s
    raw = Json.deserialize(s, User)
    if raw is User {
        println raw.name
    }
}
